# AI 구조요청 신호탐지 웹앱 작업 규칙

## 만드는 것

브라우저 마이크로 소리를 듣고 위험 신호(비명, 도움 요청, 두드리는 소리)를 감지하면 경보를 울리는 고등학교 수업용 웹앱이다.

## 꼭 지킬 것

- 애플리케이션은 `index.html`, `app.js`, `style.css` 세 파일로 구성한다. 사용 안내는 `README.md`에 둔다.
- npm이나 빌드 도구를 사용하지 않는다.
- 라이브러리는 CDN `<script>` 태그로만 가져오고 스타일은 Tailwind CDN을 사용한다.
- TensorFlow.js는 `1.3.1`, speech-commands는 `0.4.0`으로 고정한다. 버전을 올리면 Teachable Machine 오디오 모델이 열리지 않을 수 있다.
- 모델 주소와 Gemini API 키는 코드에 넣지 않고 화면에서 입력받는다.
- 화면에 보이는 글씨는 모두 한국어로 쓴다.

## 자주 틀리는 곳

- `result.scores`는 `wordLabels()`와 인덱스 순서가 대응한다. 라벨 이름으로 점수를 찾지 않는다.
- `listen()`의 `probabilityThreshold`는 `0`으로 둔다. 경보 판정 임계값은 콜백 안에서 별도로 적용한다.
- 사이렌·로그·Gemini 요청은 경보 구간에 들어가는 순간 한 번만 실행한다. 일정 시간마다 반복하지 않는다.
- `AudioContext`는 시작 버튼의 클릭 처리 안에서 만든다.
- 라벨에 `비명`, `도움`, `구조`, `살려`, `두드`, `노크`, `타격`, `help`, `scream`, `knock`이 포함되면 자동으로 경보 대상으로 선택하고, 화면의 체크박스로 수정할 수 있게 한다.
- Mock 모드는 실제 모델이나 마이크 없이도 정상 감시, 경보, 로그 기록 흐름을 전부 확인할 수 있어야 한다.

